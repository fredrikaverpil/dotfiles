#!/bin/bash

for x in *; do
    if [ -d "${x}" ]; then
        current_dir=$(pwd)
        cd "$x" || exit
        pwd
        docker compose down
        echo "-----"

        cd "$current_dir" || exit
    fi
done
