#!/bin/bash -e

for x in *; do
    if [ -d "${x}" ]; then
        current_dir=$(pwd)
        cd "$x" || exit
        pwd
        git-reset
        echo "-----"

        cd "$current_dir" || exit
    fi
done
