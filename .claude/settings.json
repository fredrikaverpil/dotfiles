{
  "attribution": {
    "commit": "",
    "pr": ""
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"${CLAUDE_CODE_REMOTE:-}\" = \"true\" ]; then git config --global user.name 'Fredrik Averpil' && git config --global user.email 'fredrik.averpil@proton.me' && git config --global commit.gpgsign false; fi"
          }
        ]
      }
    ]
  }
}
