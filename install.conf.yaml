---
- defaults:
    link:
      create: true
      relink: true
- clean: ['~', ~/.config]
- create: [~/Downloads]
- link:
    ~/.dotfiles: ''
    ~/.shell:
    ~/.bash_profile:
    ~/.bashrc_user: bashrc
    ~/.zprofile:
    ~/.zshrc_user: zshrc
    ~/.config/starship.toml: starship.toml
    ~/.gitconfig:
    ~/.gitconfig_doktor:
    ~/.gitignore_global:
    ~/.gitmessage:
    ~/.tmux.conf:
    ~/.config/yabai/yabairc: yabairc
    ~/.config/skhd/skhdrc: skhdrc
    ~/.wezterm.lua: wezterm.lua
    ~/.config/LazyVim/lua:
      path: nvim-lazyvim/lua
      force: true
    ~/.config/helix:
    ~/Library/Application Support/Code/User/settings.json:
      path: vscode_settings.jsonc
      force: true
      if: '[ "$(uname -s)" = "Darwin" ]'
    ~/Library/Application Support/Code - Insiders/User/settings.json:
      path: vscode-insiders_settings.jsonc
      force: true
      if: '[ "$(uname -s)" = "Darwin" ]'

    # Remap US English layout's ['; keys to Swedish åäö keys
    # However, this works poorly, so commented out for now
    #   ~/Library/KeyBindings/DefaultKeyBinding.dict:
    #       path: _macos/DefaultKeyBinding.dict
    #       force: true
    #       if: '[ "$(uname -s)" = "Darwin" ]'

    # Remap US English layout's ['; keys to Swedish åäö keys using Karabiner
    ~/.config/karabiner:
      path: _macos/karabiner
      create: true
      if: '[ "$(uname -s)" = "Darwin" ]'
- shell:
    - git submodule sync --recursive

    # add shell sourcing to bash
    - grep -qxF '[[ -s ~/.bashrc_user ]] && source ~/.bashrc_user' ~/.bashrc ||
      echo '[[ -s ~/.bashrc_user ]] && source ~/.bashrc_user' >> ~/.bashrc

    # add shell sourcing to zsh
    - grep -qxF '[[ -s ~/.zshrc_user ]] && source ~/.zshrc_user' ~/.zshrc ||
      echo '[[ -s ~/.zshrc_user ]] && source ~/.zshrc_user' >> ~/.zshrc

    # ssh
    # TODO: replace this with a "create" (see dotbot README for example)
    - mkdir -p ~/.ssh && chmod 700 ~/.ssh
    - touch ~/.ssh/known_hosts && chmod 644 ~/.ssh/known_hosts
    - touch ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys

    # pre-commit hook
    - cp pre-commit .git/hooks/pre-commit
