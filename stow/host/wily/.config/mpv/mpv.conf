# Xe2 decodes H.264, HEVC (including 4:2:2 10-bit camera footage), VP9 and
# AV1 in hardware. No profile=fast: 4K30 HEVC 4:2:2 plays without drops even
# in software, so default gpu-next rendering costs nothing here.
hwdec=auto-safe
