# Keeps hyprsunset inert at startup: without a profile it applies a tint of its
# own. The schedule lives in the Quickshell nightlight service instead, because
# hyprsunset only understands fixed clock times.
profile {
    time = 07:00
    identity = true
}
