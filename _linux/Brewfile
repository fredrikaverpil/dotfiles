# Usage:
# brew bundle --file=path/to/Brewfile

# For the canonical package list, refer to the Nix configuration:
# - CLI tools: nix/shared/home-manager-base.nix
# - GUI apps: nix/shared/homebrew.nix (casks and brews sections)
# - Host-specific: nix/hosts/*/darwin-configuration.nix
#
# This Brewfile is maintained for legacy/manual setups where Nix is not available.
# Manually add desired packages below by copying from the Nix configuration:

# Example:
# tap 'example/tap'
# brew 'example-package'
# cask 'example-app'
